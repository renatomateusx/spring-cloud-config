server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/renatomateusx/repoconfig
          repos:
            appFoo:
              pattern: app-foo
              uri: https://github.com/renatomateusx/repoconfig

endpoints:
  restart:
    enabled: true